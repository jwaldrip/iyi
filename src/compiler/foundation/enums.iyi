module compiler/foundation/enums

struct ::Range(B, E)
  def ===(value) : Bool
    if exclusive?
      value >= self.begin && value < self.end
    else
      value >= self.begin && value <= self.end
    end
  end
end
